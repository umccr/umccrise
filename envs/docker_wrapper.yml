# Environment for dockerized umccrise version (envoked with `--docker`) and runs snakemake pipeline withing container

name: umccrise

channels:
  - umccr
  - vladsaveliev
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - python >=3.7.3
  - click
  - ngs_utils ==2.8.9
  - nose
