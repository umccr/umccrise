name: pcgr

channels:
  - pcgr
  - vladsaveliev
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - r-openssl>=1.1
  - samtools
  - bcftools
  - click
  - ngs_utils
  - pcgr_dockerized
  - cpsr_dockerized
  - cacao_dockerized
